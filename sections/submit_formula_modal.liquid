{% comment %} <a id="click_{{ section.settings.trigger }}" href="#{{ section.settings.trigger }}" class="">Open Popup</a> {% endcomment %}

<span
  style="display:none;"
  id="click_{{ section.settings.formula_modal_toast_trigger }}"
  href="#{{ section.settings.formula_modal_toast_trigger }}"
  class="">Open Toast</span>


<div id="{{ section.settings.formula_modal_toast_trigger }}" class="toast__overlay">
  {% comment %} <div class="modal_window"></div> {% endcomment %}
  <div style="background:{{ section.settings.formula_modal_bg_color }}" class="toast__box">

    

    <p class="heading">
        {{ section.settings.formula_modal_toast_modal_title }} 
        <span class="toast__close" id="toast__close">&times;</span>
    </p>

    <div class="toast__content_div">

      <div class="toast__content">
        {{ section.settings.formula_modal_toast_modal_content }}

        {%- liquid
        assign contact_form_class = 'isolate'
        if settings.animations_reveal_on_scroll
          assign contact_form_class = 'isolate scroll-trigger animate--slide-in'
        endif
      -%}
        {%- form 'contact', id: 'ModalForm', class: contact_form_class -%}
          {%- if form.posted_successfully? -%}
            <h2 class="form-status form-status-list form__message" tabindex="-1" >  {% comment %} (left) autofocus {% endcomment %}
              {{- 'icon-success.svg' | inline_asset_content -}}
              {{ 'templates.contact.modal_form.post_success' | t }}
            </h2>
          {%- elsif form.errors -%}
            <div class="form__message">
              <h2 class="form-status caption-large text-body" role="alert" tabindex="-1" autofocus>
                <span class="svg-wrapper"> {{- 'icon-error.svg' | inline_asset_content -}}</span>
               
                {{ 'templates.contact.modal_form.error_heading' | t }}
              </h2>
            </div>
            <ul class="form-status-list caption-large" role="list">
              <li>
                
                <a href="#ModalForm-email" class="link">
                  {{ form.errors.translated_fields.email | capitalize }}
                  {{ form.errors.messages.email }}
                </a>
              </li>
            </ul>
          {%- endif -%}
          
          <p class="modal_title_txt" style="max-with:100%;margin:0 0 16px 0;color: black; font-size: 16px;">
            To create your unique formula, select up to 10 of our premium ingredients below - and choose the ratios that suit your needs. 
            {% comment %} Add your custom formula to your cart and we'll have it delivered to you in no time. {% endcomment %}
          </p>

          <div class="contact__fields">
            <div class="field">
              <input
                required
                class="field__input"
                autocomplete="name"
                type="text"
                id="ModalForm-name"
                name="contact[{{ 'templates.contact.modal_form.name' | t }}]"
                value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}"
                placeholder="{{ 'templates.contact.modal_form.name' | t }}"
              >
              <label class="field__label" for="ModalForm-name">{{ 'templates.contact.modal_form.name' | t }}</label>
            </div>
            <div class="field field--with-error">
              <input
                required
                autocomplete="email"
                type="email"
                id="ModalForm-email"
                class="field__input"
                name="contact[email]"
                spellcheck="false"
                autocapitalize="off"
                value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
                aria-required="true"
                {% if form.errors contains 'email' %}
                  aria-invalid="true"
                  aria-describedby="ModalForm-email-error"
                {% endif %}
                placeholder="{{ 'templates.contact.modal_form.email' | t }}"
              >
              <label class="field__label" for="ModalForm-email">
                {{- 'templates.contact.modal_form.email' | t }}
                <span aria-hidden="true">*</span></label
              >
              {%- if form.errors contains 'email' -%}
                <small class="contact__field-error" id="ModalForm-email-error">
                  <span class="visually-hidden">{{ 'accessibility.error' | t }}</span>
                  <span class="form__message">
                    <span class="svg-wrapper">
                      {{- 'icon-error.svg' | inline_asset_content -}}
                    </span>
                    {{- form.errors.translated_fields.email | capitalize }}
                    {{ form.errors.messages.email -}}
                  </span>
                </small>
              {%- endif -%}
            </div>
          </div>
          <div class="field">
            <input
              type="tel"
              id="ModalForm-phone"
              class="field__input"
              autocomplete="tel"
              name="contact[{{ 'templates.contact.modal_form.phone' | t }}]"
              pattern="[0-9\-]*"
              value="{% if form.phone %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.phone' | t }}"
            >
            <label class="field__label" for="ModalForm-phone">{{ 'templates.contact.modal_form.phone' | t }}</label>
          </div>

            <div class="mw_modal_title">
              <span class="modal_clr_span" style="background-color: rgb(255, 103, 0); height: 4px;"></span>
              <p class="modal_title_txt" style="margin-right:-4px;color: black; font-size: 16px;">
              Formula Tub Size 
              <div class="tooltip">
                <span class="info-icon">
                  <i class="fa-solid fa-circle-question"></i>
                </span> 
                <span class="tooltiptext">
                 Choose which size you would like your Formula supplied in... <br>
                </span>
              </div>
              </p>
              <span class="modal_clr_span" style="background-color: rgb(255, 103, 0); height: 4px;"></span>
          </div>

          {% comment %} ------- START RADIO BUTTONS --------- {% endcomment %}
          <span class="mw_formula_radio_btns" id="mw_formula_radio_btns">
                <span class="formula_size_span">
                  <p style="margin:0;min-height:78px;top: 1.8rem;object-position: center;position: relative;"><img width="28" height="auto" src="https://cdn.shopify.com/s/files/1/0713/8685/7724/files/tub_size_200g.jpg" /></p>
                  <input type="radio" 
                  id="ModalForm-200g" 
                  class=""
                  name="contact[{{ 'templates.contact.modal_form.formula_size' | t }}]" 
                  value="200g Tub" 
                  >
                  <label for="ModalForm-200g">200g</label>
                </span>
                
                <span class="formula_size_span">
                  <p style="margin:0;min-height:78px;top: 0.7rem;object-position: center;position: relative;"><img width="35" height="auto" src="https://cdn.shopify.com/s/files/1/0713/8685/7724/files/tub_size_500g.jpg" /></p>
                  <input type="radio" 
                  id="ModalForm-500g" 
                  class=""
                  name="contact[{{ 'templates.contact.modal_form.formula_size' | t }}]" 
                  value="500g Tub" 
                  >
                  <label for="ModalForm-500g">500g</label>
                </span>

                <span class="formula_size_span">
                  <p style="margin:0;min-height:78px;top: 0rem;object-position: center;position: relative;"><img width="40" height="auto" src="https://cdn.shopify.com/s/files/1/0713/8685/7724/files/tub_size_1kg.jpg" /></p>
                  <input type="radio" 
                  id="ModalForm-1kg" 
                  class=""
                  name="contact[{{ 'templates.contact.modal_form.formula_size' | t }}]" 
                  value="1kg Tub" 
                  >
                  <label for="ModalForm-1kg">1kg</label>
                </span>



          </span>
          {% comment %} ------- END RADIO BUTTONS --------- {% endcomment %}
          
          {% comment %} <p>Your Ingredient List:</p> {% endcomment %}
          <div class="mw_modal_title">
              <span class="modal_clr_span" style="background-color: rgb(255, 103, 0); height: 4px;"></span>
              <p class="modal_title_txt" style="margin-right:-4px;color: black; font-size: 16px;">
              Your Formula's Name 
              </p>
              <span class="modal_clr_span" style="background-color: rgb(255, 103, 0); height: 4px;"></span>
          </div>

          <div class="field">

            <input
            required
            class="field__input"
            autocomplete=""
            type="text"
            id="ModalForm-body"
            name="contact[{{ 'templates.contact.modal_form.comment' | t }}]"
            placeholder="{{ 'templates.contact.modal_form.comment' | t }}"
            >
            <label class="field__label" for="ModalForm-body">{{- 'templates.contact.modal_form.comment' | t -}}</label>
{% comment %} 
            <textarea
              rows="10"
              id="ModalForm-body"
              class="text-area field__input"
              name="contact[{{ 'templates.contact.modal_form.comment' | t }}]"
              placeholder="{{ 'templates.contact.modal_form.comment' | t }}"
            >
              {{- form.body -}}
            </textarea>
            <label class="form__label field__label" for="ModalForm-body">
              {{- 'templates.contact.modal_form.comment' | t -}}
            </label> {% endcomment %}
          </div>




    {% comment %} {% assign conditional_items = product.variants | where: "type", "featured" %} {% endcomment %}
    {% comment %} {% for product in collection.products %} {% endcomment %}

{% comment %}  
<select id="all_products_dropdown_1" name="selected_product_handle">
  <option value="" selected disabled>Ingredient 1</option>
  <option value="" disabled> ---- AMINO ACIDS ----- </option>

    {% for product in collections.amino-acids.products %}
        {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
        {% for variant in filtered_variants %}
            {% if forloop.last %} 
              {% assign variant_title = variant.title | downcase %}
              {% assign path_segments = variant.title | split: '/' %}
              {% assign last_segment = path_segments | last %}
              <div>
                  <h2> {{ product.title }} {{ last_segment }}</h2>
                  <p>Price: {{ variant.price | money }}</p>
                </div> 
                <option data-price="{{ product.price | money }}" data-weight="{{ last_segment }}" value="{{ product.handle }}">{{ product.title }} | {{ variant.price | money }}</option>
            {% endif %}
        {% endfor %}
    {% endfor %}

</select>
 {% endcomment %}

          {% comment %} <p>Your Ingredient List:</p> {% endcomment %}
          <div class="mw_modal_title">
              <span class="modal_clr_span" style="background-color: rgb(255, 103, 0); height: 4px;"></span>
              <p class="modal_title_txt" style="margin-right:-4px;color: black; font-size: 16px;">
              Your Ingredient List 
              <div class="tooltip">
                <span class="info-icon">
                  <i class="fa-solid fa-circle-question"></i>
                </span> 
                <span class="tooltiptext">
                 <b style="text-decoration:underline;">Example:</b> <br>
                 Pea Protein Isolate | 20% <br>
                 MCT OIl Powder | 40% <br>
                 Hemp Protein | 40% <br>
                <b>...These ingredients total 100%</b> 
                </span>
              </div>
              </p>
              <span class="modal_clr_span" style="background-color: rgb(255, 103, 0); height: 4px;"></span>
          </div>


          <div class="field">
            {% comment %} <label for="all_products_dropdown">Select a Product:</label> {% endcomment %}
            <select id="all_products_dropdown_1" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 1</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

                {% comment %}{% endcomment %}
                <span id="clear_ingredient_1" class="clear_ingredient hide_element">x</span>
                <input
                  type="hidden"
                  id="ModalForm-ingredient_1"
                  class="field__input"
                  autocomplete=""
                  name="contact[{{ 'templates.contact.modal_form.ingredient1' | t }}]"
                  value="{% if form.ingredient1 %}{{ form.ingredient1 }}{% elsif customer %}{{ customer.ingredient1 }}{% endif %}"
                  placeholder="{{ 'templates.contact.modal_form.ingredient1' | t }}"
                >
                <label class="field__label" for="ModalForm-ingredient_1">{{ 'templates.contact.modal_form.ingredient1' | t }}</label> 
                
                <select id="mw_formula_percentDropdown_1" class="mw_formula_percentDropdown_1">
                    <option selected disabled value="0">0%</option>
                    <option value="5">5%</option>
                    <option value="10">10%</option>
                    <option value="15">15%</option>
                    <option value="20">20%</option>
                    <option value="25">25%</option>
                    <option value="30">30%</option>
                    <option value="35">35%</option>
                    <option value="40">40%</option>
                    <option value="45">45%</option>
                    <option value="50">50%</option>
                    <option value="55">55%</option>
                    <option value="60">60%</option>
                    <option value="65">65%</option>
                    <option value="70">70%</option>
                    <option value="75">75%</option>
                    <option value="80">80%</option>
                    <option value="85">85%</option>
                    <option value="90">90%</option>
                    <option value="95">95%</option>
                </select>
                <input type="hidden" id="valueInput_1" placeholder="Percent" value="">
                <input type="hidden" id="valueInput_1_price" placeholder="Price" value="">
                <input type="hidden" id="valueInput_1_weight" placeholder="Weight" value="">
          </div>


          <div class="field">

          <select id="all_products_dropdown_2" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
            <option value="" selected disabled>Ingredient 2</option>
            <option value="" disabled> ---- AMINO ACIDS ----- </option>
            {% comment %} {% for product in collections.all.products %} {% endcomment %}
              {% for product in collections.amino-acids.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
            <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
            {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
              {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
              {% for variant in filtered_variants %}
                  {% if forloop.last %} 
                      {% assign variant_price = variant.price | money %}
                      {% assign product_title_for_check = product.title | downcase %}
                      {% assign path_segments = variant.title | split: '/' %}
                      {% assign first_segment = path_segments | first %}
                      {% assign last_segment = path_segments | last %}
                      {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                      {% if final_weight_calc == '1k' %} 
                        {% assign final_weight_calc = 1000 %} 
                      {% endif %}
                      {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                      <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                        {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                          {% comment %} | {{ variant_price }} {% endcomment %}
                      </option>
                  {% endif %}
              {% endfor %}
          {% endfor %}
            <option value="" disabled> ---- CREATINE ----- </option>
              {% for product in collections.creatine.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
            <option value="" disabled> ---- FATS ----- </option>
            {% for product in collections.fats.products %}
              {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
              {% for variant in filtered_variants %}
                  {% if forloop.last %} 
                      {% assign variant_price = variant.price | money %}
                      {% assign product_title_for_check = product.title | downcase %}
                      {% assign path_segments = variant.title | split: '/' %}
                      {% assign first_segment = path_segments | first %}
                      {% assign last_segment = path_segments | last %}
                      {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                      {% if final_weight_calc == '1k' %} 
                        {% assign final_weight_calc = 1000 %} 
                      {% endif %}
                      {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                      <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                        {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                          {% comment %} | {{ variant_price }} {% endcomment %}
                      </option>
                  {% endif %}
              {% endfor %}
            {% endfor %}
            <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
            <option value="" disabled> ---- HERBAL ----- </option>
            {% for product in collections.herbal.products %}
              {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
              {% for variant in filtered_variants %}
                  {% if forloop.last %} 
                      {% assign variant_price = variant.price | money %}
                      {% assign product_title_for_check = product.title | downcase %}
                      {% assign path_segments = variant.title | split: '/' %}
                      {% assign first_segment = path_segments | first %}
                      {% assign last_segment = path_segments | last %}
                      {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                      {% if final_weight_calc == '1k' %} 
                        {% assign final_weight_calc = 1000 %} 
                      {% endif %}
                      {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                      <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                        {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                          {% comment %} | {{ variant_price }} {% endcomment %}
                      </option>
                  {% endif %}
              {% endfor %}
            {% endfor %}
            <option value="" disabled> ---- PROTEIN ----- </option>
            {% for product in collections.protein.products %}
              {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
              {% for variant in filtered_variants %}
                  {% if forloop.last %} 
                      {% assign variant_price = variant.price | money %}
                      {% assign product_title_for_check = product.title | downcase %}
                      {% assign path_segments = variant.title | split: '/' %}
                      {% assign first_segment = path_segments | first %}
                      {% assign last_segment = path_segments | last %}
                      {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                      {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                      {% endif %}
                      {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                      <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                        {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                          {% comment %} | {{ variant_price }} {% endcomment %}
                      </option>
                  {% endif %}
              {% endfor %}
            {% endfor %}
            <option value="" disabled> ---- SUPERFOODS ----- </option>
            {% for product in collections.superfoods.products %}
              {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
              {% for variant in filtered_variants %}
                  {% if forloop.last %} 
                      {% assign variant_price = variant.price | money %}
                      {% assign product_title_for_check = product.title | downcase %}
                      {% assign path_segments = variant.title | split: '/' %}
                      {% assign first_segment = path_segments | first %}
                      {% assign last_segment = path_segments | last %}
                      {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                      {% if final_weight_calc == '1k' %} 
                        {% assign final_weight_calc = 1000 %} 
                      {% endif %}
                      {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                      <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                        {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                          {% comment %} | {{ variant_price }} {% endcomment %}
                      </option>
                  {% endif %}
              {% endfor %}
            {% endfor %}
          </select>

                  <span id="clear_ingredient_2" class="clear_ingredient hide_element">x</span>
                  <input
                    type="hidden"
                    id="ModalForm-ingredient_2"
                    class="field__input"
                    autocomplete=""
                    name="contact[{{ 'templates.contact.modal_form.ingredient2' | t }}]"
                    value="{% if form.ingredient2 %}{{ form.ingredient2 }}{% elsif customer %}{{ customer.ingredient2 }}{% endif %}"
                    placeholder="{{ 'templates.contact.modal_form.ingredient3' | t }}"
                  >
                  <label class="field__label" for="ModalForm-ingredient_2">{{ 'templates.contact.modal_form.ingredient2' | t }}</label>
                  
                  <select id="mw_formula_percentDropdown_2" class="mw_formula_percentDropdown_1">
                    <option selected disabled value="0">0%</option>
                    <option value="5">5%</option>
                    <option value="10">10%</option>
                    <option value="15">15%</option>
                    <option value="20">20%</option>
                    <option value="25">25%</option>
                    <option value="30">30%</option>
                    <option value="35">35%</option>
                    <option value="40">40%</option>
                    <option value="45">45%</option>
                    <option value="50">50%</option>
                    <option value="55">55%</option>
                    <option value="60">60%</option>
                    <option value="65">65%</option>
                    <option value="70">70%</option>
                    <option value="75">75%</option>
                    <option value="80">80%</option>
                    <option value="85">85%</option>
                    <option value="90">90%</option>
                    <option value="95">95%</option>
                </select>
                <input type="hidden" id="valueInput_2" placeholder="Percent" value="">
                <input type="hidden" id="valueInput_2_price" placeholder="Price" value="">
                <input type="hidden" id="valueInput_2_weight" placeholder="Weight" value="">
          </div>

          <div class="field">
                <select id="all_products_dropdown_3" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
                  <option value="" selected disabled>Ingredient 3</option>
                  <option value="" disabled> ---- AMINO ACIDS ----- </option>
                  {% comment %} {% for product in collections.all.products %} {% endcomment %}
                    {% for product in collections.amino-acids.products %}
                        {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                        {% for variant in filtered_variants %}
                            {% if forloop.last %} 
                              {% assign variant_price = variant.price | money %}
                              {% assign product_title_for_check = product.title | downcase %}
                              {% assign path_segments = variant.title | split: '/' %}
                              {% assign first_segment = path_segments | first %}
                              {% assign last_segment = path_segments | last %}
                              {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                              {% if final_weight_calc == '1k' %} 
                                {% assign final_weight_calc = 1000 %} 
                              {% endif %}
                               {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                                <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                                  {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                    {% comment %} | {{ variant_price }} {% endcomment %}
                                </option>
                            {% endif %}
                        {% endfor %}
                    {% endfor %}
                  <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
                  {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            {% comment %} <div>
                              <h2> {{ product.title }} {{ last_segment }}</h2>
                              <p>Price: {{ variant.price | money }}</p>
                            </div> {% endcomment %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
                  <option value="" disabled> ---- CREATINE ----- </option>
                    {% for product in collections.creatine.products %}
                      {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                      {% for variant in filtered_variants %}
                          {% if forloop.last %} 
                              {% assign variant_price = variant.price | money %}
                              {% assign product_title_for_check = product.title | downcase %}
                              {% assign path_segments = variant.title | split: '/' %}
                              {% assign first_segment = path_segments | first %}
                              {% assign last_segment = path_segments | last %}
                              {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                              {% if final_weight_calc == '1k' %} 
                                {% assign final_weight_calc = 1000 %} 
                              {% endif %}
                              {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                              <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                                {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                  {% comment %} | {{ variant_price }} {% endcomment %}
                              </option>
                          {% endif %}
                      {% endfor %}
                  {% endfor %}
                  <option value="" disabled> ---- FATS ----- </option>
                  {% for product in collections.fats.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                  {% endfor %}
                  <option value="" disabled> ---- FLAVOURS ----- </option>
                  {% for product in collections.flavours.products %}
                    {% if product.has_only_default_variant %}
                      {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                        {% assign variant_price = product.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign my_flavour_title_string = product.title %}
                        {% assign path_segments = product.title | split: ' ' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign title_without_weight = product.title | remove: last_segment  %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ title_without_weight }}
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% else %}
                      {% comment %} <p>This product has variants.</p> {% endcomment %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                    {% endif %}
                  {% endfor %}
                  <option value="" disabled> ---- HERBAL ----- </option>
                  {% for product in collections.herbal.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                  {% endfor %}
                  <option value="" disabled> ---- PROTEIN ----- </option>
                  {% for product in collections.protein.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                  {% endfor %}
                  <option value="" disabled> ---- SUPERFOODS ----- </option>
                  {% for product in collections.superfoods.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                  {% endfor %}
                </select>

                  <span id="clear_ingredient_3" class="clear_ingredient hide_element">x</span>
                  <input
                    type="hidden"
                    id="ModalForm-ingredient_3"
                    class="field__input"
                    autocomplete=""
                    name="contact[{{ 'templates.contact.modal_form.ingredient3' | t }}]"
                    value="{% if form.ingredient3 %}{{ form.ingredient3 }}{% elsif customer %}{{ customer.ingredient3 }}{% endif %}"
                    placeholder="{{ 'templates.contact.modal_form.ingredient3' | t }}"
                  >
                  <label class="field__label" for="ModalForm-ingredient_3">{{ 'templates.contact.modal_form.ingredient3' | t }}</label>

                  <select id="mw_formula_percentDropdown_3" class="mw_formula_percentDropdown_1">
                    <option selected disabled value="0">0%</option>
                    <option value="5">5%</option>
                    <option value="10">10%</option>
                    <option value="15">15%</option>
                    <option value="20">20%</option>
                    <option value="25">25%</option>
                    <option value="30">30%</option>
                    <option value="35">35%</option>
                    <option value="40">40%</option>
                    <option value="45">45%</option>
                    <option value="50">50%</option>
                    <option value="55">55%</option>
                    <option value="60">60%</option>
                    <option value="65">65%</option>
                    <option value="70">70%</option>
                    <option value="75">75%</option>
                    <option value="80">80%</option>
                    <option value="85">85%</option>
                    <option value="90">90%</option>
                    <option value="95">95%</option>
                  </select>
                  <input type="hidden" id="valueInput_3" placeholder="Percent" value="">
                  <input type="hidden" id="valueInput_3_price" placeholder="Price" value="">
                  <input type="hidden" id="valueInput_3_weight" placeholder="Weight" value="">
          </div>

          <div class="field">
            <select id="all_products_dropdown_4" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 4</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

              <span id="clear_ingredient_4" class="clear_ingredient hide_element">x</span>
            <input
              type="hidden"
              id="ModalForm-ingredient_4"
              class="field__input"
              autocomplete=""
              name="contact[{{ 'templates.contact.modal_form.ingredient4' | t }}]"
              value="{% if form.ingredient4 %}{{ form.ingredient4 }}{% elsif customer %}{{ customer.ingredient4 }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.ingredient4' | t }}"
            >
            <label class="field__label" for="ModalForm-ingredient_4">{{ 'templates.contact.modal_form.ingredient4' | t }}</label>
            
            <select id="mw_formula_percentDropdown_4" class="mw_formula_percentDropdown_1">
              <option selected disabled value="0">0%</option>
              <option value="5">5%</option>
              <option value="10">10%</option>
              <option value="15">15%</option>
              <option value="20">20%</option>
              <option value="25">25%</option>
              <option value="30">30%</option>
              <option value="35">35%</option>
              <option value="40">40%</option>
              <option value="45">45%</option>
              <option value="50">50%</option>
              <option value="55">55%</option>
              <option value="60">60%</option>
              <option value="65">65%</option>
              <option value="70">70%</option>
              <option value="75">75%</option>
              <option value="80">80%</option>
              <option value="85">85%</option>
              <option value="90">90%</option>
              <option value="95">95%</option>
            </select>
            <input type="hidden" id="valueInput_4" placeholder="Percent" value="">
            <input type="hidden" id="valueInput_4_price" placeholder="Price" value="">
            <input type="hidden" id="valueInput_4_weight" placeholder="Weight" value="">
          </div>
          
          <div class="field">
            <select id="all_products_dropdown_5" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 5</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

              <span id="clear_ingredient_5" class="clear_ingredient hide_element">x</span>
            <input
              type="hidden"
              id="ModalForm-ingredient_5"
              class="field__input"
              autocomplete=""
              name="contact[{{ 'templates.contact.modal_form.ingredient5' | t }}]"
              value="{% if form.ingredient5 %}{{ form.ingredient5 }}{% elsif customer %}{{ customer.ingredient5 }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.ingredient5' | t }}"
            >
            <label class="field__label" for="ModalForm-ingredient_5">{{ 'templates.contact.modal_form.ingredient5' | t }}</label>
           
            <select id="mw_formula_percentDropdown_5" class="mw_formula_percentDropdown_1">
              <option selected disabled value="0">0%</option>
              <option value="5">5%</option>
              <option value="10">10%</option>
              <option value="15">15%</option>
              <option value="20">20%</option>
              <option value="25">25%</option>
              <option value="30">30%</option>
              <option value="35">35%</option>
              <option value="40">40%</option>
              <option value="45">45%</option>
              <option value="50">50%</option>
              <option value="55">55%</option>
              <option value="60">60%</option>
              <option value="65">65%</option>
              <option value="70">70%</option>
              <option value="75">75%</option>
              <option value="80">80%</option>
              <option value="85">85%</option>
              <option value="90">90%</option>
              <option value="95">95%</option>
            </select>
            <input type="hidden" id="valueInput_5" placeholder="Percent" value="">
            <input type="hidden" id="valueInput_5_price" placeholder="Price" value="">
            <input type="hidden" id="valueInput_5_weight" placeholder="Weight" value="">
          </div>

          <div class="field">
            <select id="all_products_dropdown_6" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 6</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

              <span id="clear_ingredient_6" class="clear_ingredient hide_element">x</span>
            <input
              type="hidden"
              id="ModalForm-ingredient_6"
              class="field__input"
              autocomplete=""
              name="contact[{{ 'templates.contact.modal_form.ingredient6' | t }}]"
              value="{% if form.ingredient6 %}{{ form.ingredient6 }}{% elsif customer %}{{ customer.ingredient6 }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.ingredient6' | t }}"
            >
            <label class="field__label" for="ModalForm-ingredient_6">{{ 'templates.contact.modal_form.ingredient6' | t }}</label>
           
            <select id="mw_formula_percentDropdown_6" class="mw_formula_percentDropdown_1">
              <option selected disabled value="0">0%</option>
              <option value="5">5%</option>
              <option value="10">10%</option>
              <option value="15">15%</option>
              <option value="20">20%</option>
              <option value="25">25%</option>
              <option value="30">30%</option>
              <option value="35">35%</option>
              <option value="40">40%</option>
              <option value="45">45%</option>
              <option value="50">50%</option>
              <option value="55">55%</option>
              <option value="60">60%</option>
              <option value="65">65%</option>
              <option value="70">70%</option>
              <option value="75">75%</option>
              <option value="80">80%</option>
              <option value="85">85%</option>
              <option value="90">90%</option>
              <option value="95">95%</option>
            </select>
            <input type="hidden" id="valueInput_6" placeholder="Percent" value="">
            <input type="hidden" id="valueInput_6_price" placeholder="Price" value="">
            <input type="hidden" id="valueInput_6_weight" placeholder="Weight" value="">
          </div>

          <div class="field">
            <select id="all_products_dropdown_7" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 7</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

              <span id="clear_ingredient_7" class="clear_ingredient hide_element">x</span>
            <input
              type="hidden"
              id="ModalForm-ingredient_7"
              class="field__input"
              autocomplete=""
              name="contact[{{ 'templates.contact.modal_form.ingredient7' | t }}]"
              value="{% if form.ingredient7 %}{{ form.ingredient7 }}{% elsif customer %}{{ customer.ingredient7 }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.ingredient7' | t }}"
            >
            <label class="field__label" for="ModalForm-ingredient_7">{{ 'templates.contact.modal_form.ingredient7' | t }}</label>
           
            <select id="mw_formula_percentDropdown_7" class="mw_formula_percentDropdown_1">
              <option selected disabled value="0">0%</option>
              <option value="5">5%</option>
              <option value="10">10%</option>
              <option value="15">15%</option>
              <option value="20">20%</option>
              <option value="25">25%</option>
              <option value="30">30%</option>
              <option value="35">35%</option>
              <option value="40">40%</option>
              <option value="45">45%</option>
              <option value="50">50%</option>
              <option value="55">55%</option>
              <option value="60">60%</option>
              <option value="65">65%</option>
              <option value="70">70%</option>
              <option value="75">75%</option>
              <option value="80">80%</option>
              <option value="85">85%</option>
              <option value="90">90%</option>
              <option value="95">95%</option>
            </select>
            <input type="hidden" id="valueInput_7" placeholder="Percent" value="">
            <input type="hidden" id="valueInput_7_price" placeholder="Price" value="">
            <input type="hidden" id="valueInput_7_weight" placeholder="Weight" value="">
          </div>


          <div class="field">
            <select id="all_products_dropdown_8" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 8</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

              <span id="clear_ingredient_8" class="clear_ingredient hide_element">x</span>
            <input
              type="hidden"
              id="ModalForm-ingredient_8"
              class="field__input"
              autocomplete=""
              name="contact[{{ 'templates.contact.modal_form.ingredient8' | t }}]"
              value="{% if form.ingredient8 %}{{ form.ingredient8 }}{% elsif customer %}{{ customer.ingredient8 }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.ingredient8' | t }}"
            >
            <label class="field__label" for="ModalForm-ingredient_8">{{ 'templates.contact.modal_form.ingredient8' | t }}</label>
           
            <select id="mw_formula_percentDropdown_8" class="mw_formula_percentDropdown_1">
              <option selected disabled value="0">0%</option>
              <option value="5">5%</option>
              <option value="10">10%</option>
              <option value="15">15%</option>
              <option value="20">20%</option>
              <option value="25">25%</option>
              <option value="30">30%</option>
              <option value="35">35%</option>
              <option value="40">40%</option>
              <option value="45">45%</option>
              <option value="50">50%</option>
              <option value="55">55%</option>
              <option value="60">60%</option>
              <option value="65">65%</option>
              <option value="70">70%</option>
              <option value="75">75%</option>
              <option value="80">80%</option>
              <option value="85">85%</option>
              <option value="90">90%</option>
              <option value="95">95%</option>
            </select>
            <input type="hidden" id="valueInput_8" placeholder="Percent" value="">
            <input type="hidden" id="valueInput_8_price" placeholder="Price" value="">
            <input type="hidden" id="valueInput_8_weight" placeholder="Weight" value="">
          </div>

          <div class="field">
            <select id="all_products_dropdown_9" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 9</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

              <span id="clear_ingredient_9" class="clear_ingredient hide_element">x</span>
            <input
              type="hidden"
              id="ModalForm-ingredient_9"
              class="field__input"
              autocomplete=""
              name="contact[{{ 'templates.contact.modal_form.ingredient9' | t }}]"
              value="{% if form.ingredient9 %}{{ form.ingredient9 }}{% elsif customer %}{{ customer.ingredient9 }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.ingredient9' | t }}"
            >
            <label class="field__label" for="ModalForm-ingredient_9">{{ 'templates.contact.modal_form.ingredient9' | t }}</label>
           
            <select id="mw_formula_percentDropdown_9" class="mw_formula_percentDropdown_1">
              <option selected disabled value="0">0%</option>
              <option value="5">5%</option>
              <option value="10">10%</option>
              <option value="15">15%</option>
              <option value="20">20%</option>
              <option value="25">25%</option>
              <option value="30">30%</option>
              <option value="35">35%</option>
              <option value="40">40%</option>
              <option value="45">45%</option>
              <option value="50">50%</option>
              <option value="55">55%</option>
              <option value="60">60%</option>
              <option value="65">65%</option>
              <option value="70">70%</option>
              <option value="75">75%</option>
              <option value="80">80%</option>
              <option value="85">85%</option>
              <option value="90">90%</option>
              <option value="95">95%</option>
            </select>
            <input type="hidden" id="valueInput_9" placeholder="Percent" value="">
            <input type="hidden" id="valueInput_9_price" placeholder="Price" value="">
            <input type="hidden" id="valueInput_9_weight" placeholder="Weight" value="">
          </div>

          <div class="field">
            <select id="all_products_dropdown_10" name="selected_product_handle" class="field__input mw_formula_productDropdown_1">
              <option value="" selected disabled>Ingredient 10</option>
              <option value="" disabled> ---- AMINO ACIDS ----- </option>
              {% comment %} {% for product in collections.all.products %} {% endcomment %}
                {% for product in collections.amino-acids.products %}
                    {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                    {% for variant in filtered_variants %}
                        {% if forloop.last %} 
                            {% assign variant_price = variant.price | money %}
                            {% assign product_title_for_check = product.title | downcase %}
                            {% assign path_segments = variant.title | split: '/' %}
                            {% assign first_segment = path_segments | first %}
                            {% assign last_segment = path_segments | last %}
                            {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                            {% if final_weight_calc == '1k' %} 
                              {% assign final_weight_calc = 1000 %} 
                            {% endif %}
                            {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                            <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                              {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                                {% comment %} | {{ variant_price }} {% endcomment %}
                            </option>
                        {% endif %}
                    {% endfor %}
                {% endfor %}
              <option value="" disabled> ---- CARBS | FIBRE | SWEETNERS ----- </option>
              {% for product in collections.carbohydrates-fibre-and-sweeteners.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
            {% endfor %}
              <option value="" disabled> ---- CREATINE ----- </option>
                {% for product in collections.creatine.products %}
                  {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                  {% for variant in filtered_variants %}
                      {% if forloop.last %} 
                          {% assign variant_price = variant.price | money %}
                          {% assign product_title_for_check = product.title | downcase %}
                          {% assign path_segments = variant.title | split: '/' %}
                          {% assign first_segment = path_segments | first %}
                          {% assign last_segment = path_segments | last %}
                          {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                          {% if final_weight_calc == '1k' %} 
                            {% assign final_weight_calc = 1000 %} 
                          {% endif %}
                          {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                          <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                            {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                              {% comment %} | {{ variant_price }} {% endcomment %}
                          </option>
                      {% endif %}
                  {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FATS ----- </option>
              {% for product in collections.fats.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- FLAVOURS ----- </option>
              {% for product in collections.flavours.products %}
                {% if product.has_only_default_variant %}
                  {% comment %} <p>This product does not have variants.</p> {% endcomment %}
                    {% assign variant_price = product.price | money %}
                    {% assign product_title_for_check = product.title | downcase %}
                    {% assign my_flavour_title_string = product.title %}
                    {% assign path_segments = product.title | split: ' ' %}
                    {% assign first_segment = path_segments | first %}
                    {% assign last_segment = path_segments | last %}
                    {% assign title_without_weight = product.title | remove: last_segment  %}
                    {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                    {% if final_weight_calc == '1k' %} 
                      {% assign final_weight_calc = 1000 %} 
                    {% endif %}
                    {% assign variant_price_per_gram = product.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                    <option data-price="{{ variant_price }}" data-variant-name="{{ my_flavour_title_string }}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                      {{ title_without_weight }}
                        {% comment %} | {{ variant_price }} {% endcomment %}
                    </option>
                {% else %}
                  {% comment %} <p>This product has variants.</p> {% endcomment %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
                {% endif %}
              {% endfor %}
              <option value="" disabled> ---- HERBAL ----- </option>
              {% for product in collections.herbal.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- PROTEIN ----- </option>
              {% for product in collections.protein.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
              <option value="" disabled> ---- SUPERFOODS ----- </option>
              {% for product in collections.superfoods.products %}
                {% assign filtered_variants = product.variants | where: "option1", "Powder" %}
                {% for variant in filtered_variants %}
                    {% if forloop.last %} 
                        {% assign variant_price = variant.price | money %}
                        {% assign product_title_for_check = product.title | downcase %}
                        {% assign path_segments = variant.title | split: '/' %}
                        {% assign first_segment = path_segments | first %}
                        {% assign last_segment = path_segments | last %}
                        {% assign final_weight_calc = last_segment | remove: "g" | remove: "lb"  | remove: "kg" | remove: "oz" | strip %}
                        {% if final_weight_calc == '1k' %} 
                          {% assign final_weight_calc = 1000 %} 
                        {% endif %}
                        {% assign variant_price_per_gram = variant.price | divided_by: final_weight_calc | divided_by: 100.00  %}
                        <option data-price="{{ variant_price }}" data-variant-name="{{ product.title }}{%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %}" data-price_pg="{{ variant_price_per_gram }}" data-weight="{{ final_weight_calc }}" value="{{ product.handle }}">
                          {{ product.title }} {%- if product_title_for_check contains 'powder' -%} {% else %} {{ first_segment }} {% endif %} 
                            {% comment %} | {{ variant_price }} {% endcomment %}
                        </option>
                    {% endif %}
                {% endfor %}
              {% endfor %}
            </select>

              <span id="clear_ingredient_10" class="clear_ingredient hide_element">x</span>
            <input
              type="hidden"
              id="ModalForm-ingredient_10"
              class="field__input"
              autocomplete=""
              name="contact[{{ 'templates.contact.modal_form.ingredient10' | t }}]"
              value="{% if form.ingredient10 %}{{ form.ingredient10 }}{% elsif customer %}{{ customer.ingredient10 }}{% endif %}"
              placeholder="{{ 'templates.contact.modal_form.ingredient10' | t }}"
            >
            <label class="field__label" for="ModalForm-ingredient_10">{{ 'templates.contact.modal_form.ingredient10' | t }}</label>
           
            <select id="mw_formula_percentDropdown_10" class="mw_formula_percentDropdown_1">
              <option selected disabled value="0">0%</option>
              <option value="5">5%</option>
              <option value="10">10%</option>
              <option value="15">15%</option>
              <option value="20">20%</option>
              <option value="25">25%</option>
              <option value="30">30%</option>
              <option value="35">35%</option>
              <option value="40">40%</option>
              <option value="45">45%</option>
              <option value="50">50%</option>
              <option value="55">55%</option>
              <option value="60">60%</option>
              <option value="65">65%</option>
              <option value="70">70%</option>
              <option value="75">75%</option>
              <option value="80">80%</option>
              <option value="85">85%</option>
              <option value="90">90%</option>
              <option value="95">95%</option>
            </select>
            <input type="hidden" id="valueInput_10" placeholder="Percent" value="">
            <input type="hidden" id="valueInput_10_price" placeholder="Price" value="">
            <input type="hidden" id="valueInput_10_weight" placeholder="Weight" value="">
          </div>

          <div style="display:none;" class="formula_checkbox_wrapper">
            <input checked type="checkbox" id="myFormulaCheckbox" name="myFormulaCheckbox" required>
            <label for="myFormulaCheckbox">I agree to the <a href="#" target="_blank">terms and conditions</a></label>
          </div>




          <div class="center contact__button">
            <button type="submit" class="color-scheme-5 gradient submit_formula_btn button">
               {{ 'templates.contact.modal_form.send' | t }} 
            </button>
          </div>

        {%- endform -%}
      </div>

    
    </div>
  <span id="form_help" class="form_help form_help_hide">Form Help</span>
  <span id="totals_container" class="form_help">Totals</span>
  </div>
</div>





{% schema %}
  {
    "name": "Submit Formula Modal",
    "settings": [
      {
        "type": "color",
        "id": "formula_modal_bg_color",
        "label": "Background Color",
        "default": "#fff"
      }, {
        "id": "formula_modal_toast_trigger",
        "type": "text",
        "label": "Modal ID",
        "info": "Unique ID for pop-up trigger"
      }, {
        "id": "formula_modal_toast_modal_title",
        "type": "text",
        "label": "Title"
      }, {
        "id": "formula_modal_toast_modal_content",
        "type": "richtext",
        "label": "Content"
      }
    ],
    "presets": [
      {
        "name": "Submit Formula Modal",
        "category": "Window"
      }
    ]
  }
{% endschema %}


{% stylesheet %}
  #ModalForm {
    padding: 0rem 0 6rem 0;
  }
  .modal_window {
    display: flex;
    flex-direction: column;
    width: 100%;
    justify-content: center;
    align-items: center;
    transition: all ease 0.5s;
  }
  #submit_formula.toast__overlay {
    align-items: center;
    padding-top: 2rem !important;
  }
  .field__input:focus, .field__input:not(:placeholder-shown), .field__input:-webkit-autofill, .customer .field input:focus, .customer .field input:not(:placeholder-shown), .customer .field input:-webkit-autofill {
    padding: 2.2rem 1.5rem 0.8rem 5rem;
    margin: var(--inputs-border-width);
}
  .toast__overlay {
    height: 100vh;
    position: fixed;
    top: 0;
    bottom: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: center;
    margin-bottom: 20px;
    align-items: end;
    background: rgb(0, 0, 0, 0.5);
    
    transition: opacity 500ms ease-in-out;
    visibility: hidden;
    opacity: 0;
    transition: all ease 0.5s;
  }

  .toast__overlay:target {
    visibility: visible;
    opacity: 1;
    z-index: 99;
    transition: all ease 0.5s;
  }

  .toast_overlay_edit_show {
    visibility: visible !important;
    opacity: 1 !important;
    z-index: 999 !important;
    transition: all ease 0.5s !important;
  }

  .popup__image {
    width: 100%;
    height: 300px;
    position: relative;
    transition: all ease 0.5s;
    text-align: center;
  }

  .toast__content_div {
    padding: 0 1.5rem;
  }

  #submit_formula .toast__box {
    overflow: scroll;
    border: 1px solid #ffffff;
    background: #fff;
    border-radius: 8px;
    width: 50%;
    height: 90%;
    position: relative;
    transition: all ease .5s;
    text-align: center;
  }

  #submit_formula .toast__box .heading {
    height: 40px;
    display: block;
    position: sticky;
    top: 0px;
    z-index: 9999;
    align-items: center;
    align-content: center;
    color: #fff;
    text-shadow: 0 0 3px #111;
    text-align: left;
    background: linear-gradient(180deg, #fe5900, #cc4700, #9c3600) !important;
    padding: 0 0 0 12px;
    font-size: 1.1em;
    font-weight: 700;
    margin-top: 0;
    margin-bottom: 0;
}

  #submit_formula .toast__content p {
    text-align:center;
    font-weight:600;
  }
  #submit_formula .toast__content h3 {
    text-align:center;
  }

  .toast__box .heading {
    height: 40px;
    display: flex;
    align-items: center;
    color: #ffffff;
    text-shadow: 0 0 3px #111;
    text-align: left;
    background: linear-gradient(180deg, rgba(254, 89, 0, 1) 0%, rgba(204, 71, 0, 1) 50%, rgba(156, 54, 0, 1) 100%) !important;
    padding: 0 0 0 12px;
    font-size: 1.1em;
    font-weight: 700;
    margin-top: 0;
    margin-bottom: 0;
  }

  .toast__close {
    position: absolute;
    z-index: 9;
    top: 6px;
    right: 8px;
    transition: all 200ms ease-out;
    color: #ffffff;
    font-size: 24px;
    height: 26px;
    border-radius: 50%;
    width: 26px;
    background: #3333337d;
    font-weight: 300;
    line-height: 25px;
    text-decoration: none;
    cursor: pointer;
  }

  .toast__close:hover {
    color: #c1c1c1;
  }

  #submit_formula .toast__close {
    position: sticky;
    top: 6px;
    right: 1rem;
    justify-content: center;
    float: right;
    display: flex;
    transition: 200ms ease-out;
    color: rgb(255, 255, 255);
    font-size: 24px;
    height: 26px;
    border-radius: 50%;
    width: 26px;
    background: rgba(51, 51, 51, 0.49);
    font-weight: 300;
    line-height: 25px;
    text-decoration: none;
    cursor: pointer;
    z-index: 99999;
}

  .toast__content {
    max-height: 30%;
    overflow: auto;
    text-align: left;
  }

  .toast__content p {
    margin: 12px auto;
  }

  .toast__content .svg-wrapper {
    animation-name: fadeIn;
    animation-duration: 1.5s;
    animation-iteration-count: infinite;
    animation-direction: alternate;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    width: 20px;
    height: 20px;
    position: relative;
    top: 5px;
    margin-right: 4px;
  }

  .toast__box {
    overflow: hidden;
    border: 1px solid #ffffff;
    background: #fff;
    border-radius: 8px;
    width: 70%;
    height: auto;
    position: relative;
    transition: all ease 0.5s;
    text-align: center;
  }

  @media screen and (max-width: 479px) {
    #submit_formula.toast__overlay {
      align-items: flex-start !important;
      padding-top: 20px;
      overflow-y: auto;
    }
    #submit_formula .toast__box {
      width: 90% !important;
    }
    #submit_formula .toast__box {
      overflow: scroll;
      border: 1px solid #ffffff;
      background: #fff;
      border-radius: 8px;
      width: 50%;
      height: auto;
      position: relative;
      transition: all ease .5s;
      text-align: center;
    }
  }

  @media screen and (max-width: 989px) {
    #submit_formula .toast__box {
      width: 80%;
    }
  }

  @media screen and (max-width: 720px) {
    .toast__content_div {
      padding: 0 10px;
    }
    .toast__content p {
      margin: 8px auto 11px;
    }
    .toast__overlay {
      position: fixed;
      top: 0;
      padding-bottom: 2rem;
      bottom: 0;
      left: 0;
      height: 100%;
      right: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      background: rgba(0, 0, 0, 0.5);
      transition: opacity 500ms ease-in-out;
      visibility: hidden;
      opacity: 0;
      transition: all ease 0.5s;
    }
    .toast__box {
      width: 90%;
    }
  }

  @media screen and (min-width: 1280px) {
    .toast__box {
      width: 60%;
    }
  }

  @media screen and (min-width: 1600px) {
    .toast__box {
      width: 30%;
    }
  }
{% endstylesheet %}

{% javascript %}
{% endjavascript %}